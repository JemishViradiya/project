const boxPaddingProps = {
  paddingTop: 2,
  paddingRight: 4,
  paddingBottom: 2,
  paddingLeft: 4,
}

const boxFlexCenterProps = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
}

const boxFlexBetweenProps = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
}

const boxFlexEndProps = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'flex-end',
}

export { boxPaddingProps, boxFlexCenterProps, boxFlexBetweenProps, boxFlexEndProps }
