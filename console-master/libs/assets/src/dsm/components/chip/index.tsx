/* eslint-disable @typescript-eslint/no-unused-vars */
import React from 'react'

import { BasicClose } from '../../../icons'

export const defaultChipProps: Record<string, unknown> = {
  // TODO: Uncomment when switching back to blue theme (Cylance doesn't use custom close icon)
  // deleteIcon: <BasicClose />,
}
