# platform/policy-common

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `yarn test platform/policy-common` to execute the unit tests via [Jest](https://jestjs.io).
