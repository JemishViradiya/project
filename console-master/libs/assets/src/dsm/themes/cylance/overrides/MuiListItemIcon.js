const MuiListItemIcon = {
  root: {
    // this reduces the amount of space in a List when a something like
    // a checkbox is used. There's a chance it may affect other uses
    // of a list and an icon, so another solution might be better.
    minWidth: '40px',
    color: 'inherit',
  },
}

export default MuiListItemIcon
