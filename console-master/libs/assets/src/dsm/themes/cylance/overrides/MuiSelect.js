const MuiSelect = ({ spacing }) => ({
  root: {
    lineHeight: '1.25rem',
  },
  select: {
    minWidth: `${spacing(6)}px`,
  },
  selectMenu: {
    minHeight: 'unset',
  },
})

export default MuiSelect
