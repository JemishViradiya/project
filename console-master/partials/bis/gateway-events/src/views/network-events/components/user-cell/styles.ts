import makeStyles from '@material-ui/core/styles/makeStyles'

export const useStyles = makeStyles((theme: any /* UesTheme */) => ({
  link: {
    color: theme.palette.link.default.main,
    textDecoration: 'none',
  },
}))
