import type { ButtonProps } from '@material-ui/core'

export const activeButtonGroupItemProps: Partial<ButtonProps> = { color: 'secondary', variant: 'contained' }
export const inactiveButtonGroupItemProps: Partial<ButtonProps> = { color: 'default' }
