const MuiPickersClockPointer = ({ palette }) => ({
  thumb: {
    borderColor: palette.secondary.main,
  },
  pointer: {
    backgroundColor: palette.secondary.main,
  },
})

export default MuiPickersClockPointer
