export {
  clearCompileError,
  clearRuntimeErrors,
  showCompileError,
  showRuntimeErrors,
} from '@pmmmwh/react-refresh-webpack-plugin/overlay'

export function handleRuntimeError(error) {
  // avoid error overlay
}
