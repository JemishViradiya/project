const MuiFormControl = ({ spacing }) => ({
  root: {
    marginBottom: `${spacing(6)}px`,
  },
  marginNormal: {
    marginTop: spacing(3),
    marginBottom: spacing(3),
  },
})

export default MuiFormControl
